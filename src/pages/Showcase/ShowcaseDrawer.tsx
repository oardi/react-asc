import React from 'react';
import { withOptions } from './components';

const ShowcaseDrawerBase = () => {
	return (
		<div>
			TODO
		</div>
	);
}

export const ShowcaseDrawer = withOptions(ShowcaseDrawerBase);
